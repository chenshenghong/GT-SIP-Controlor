# Dependencies
node_modules/

# Build output
out/
dist/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# Vendor firmware source (factory-provided, keep local — do not publish)
docs/firmware-reference/

# Knowledge graph artifacts (graphify)
graphify-out/

# GitNexus local index
.gitnexus/
# Local code intelligence artifacts
semantic-vector-index/
scripts/node_modules/

# Personal Claude Code settings (machine-specific)
.claude/settings.local.json

# hermes byproducts — must not land in repo (may contain factory docs)
.hermes/

# Factory delivery / test packages (binary deliverables, keep local)
GT-SIP-GW-*.zip

# Spec-book project (kept local — factory-adjacent, not published)
GT-SIP-GW-規格書/

# TypeScript incremental build info
*.tsbuildinfo
